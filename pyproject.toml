[project]
name = "genbrick"
version = "0.1.0"
requires-python = ">=3.11"

[project.optional-dependencies]
test = ["pytest", "playwright", "pytest-playwright"]

[tool.setuptools]
packages = ["pipeline"]

[tool.pytest.ini_options]
testpaths = ["tests"]
